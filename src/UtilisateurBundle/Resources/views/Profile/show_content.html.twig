{% trans_default_domain 'FOSUserBundle' %}
<div class="login changement">
    <p>Mon Profile</p>
    <div class="logo-login"><img src="{{ asset('img/logo.jpg') }}">  
	    <div class="fos_user_user_show">

	        <div> 
	        	<label>{{ 'profile.show.username'|trans }} </label> : 
	        	<input type="text" id="username" name="_username" value="{{ user.username }}" disabled />
	        </div>
	        <div> 
	        	<label> {{ 'profile.show.email'|trans }} </label> : 
	        	<input type="text" id="username" name="_username" value="{{ user.email }}" disabled />
	        </div>

	        <a class="btn-oublier" href="{{ path('fos_user_profile_edit') }}">Modifier mon profile</a>
	    </div>
    </div>
</div>
