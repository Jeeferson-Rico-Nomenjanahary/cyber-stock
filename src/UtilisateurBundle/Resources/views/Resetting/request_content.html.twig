{% trans_default_domain 'FOSUserBundle' %}

<form action="{{ path('fos_user_resetting_send_email') }}" method="POST" class="fos_user_resetting_request">
    <div class="login">
    	<p>GESTION DE STOCK</p>
    	<div class="logo-login"><img src="{{ asset('img/logo.jpg') }}">  </div>
	    <div class="reinitialisation">
	        <label class="LB-reinitialisation" for="username">{{ 'resetting.request.username'|trans }}</label>
	        <input class="I-reinitialisation" type="text" id="username" name="username" required="required" />
	    </div>
	    <div align="center">
	        <input class="btn-connex BTN-reinitialisation" type="submit" value="{{ 'resetting.request.submit'|trans }}" />
	    </div>
	</div>
</form>

