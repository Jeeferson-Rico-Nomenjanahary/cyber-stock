{{ dump (stocks) }}
{#<div id="stock" class="tabcontent " style="display: block;">
    <form action="{{ path('stock_stock_index') }}" method="GET">
        <div class="contenu-recherche">
            <fieldset>
                <legend>Recherche</legend>
                <div style="display: inline-block;">
                    <span class="form-label-content">Article</span>
                    <input  type="text"  id="Article" name="filters[name]" value="{% if currentFilters['name'] is defined %}{{ currentFilters['name'] }}{% endif %}">
                    <label for="Article" class="form-label">

                    </label>
                </div>
                <button class="btn btn-first" type="submit" value=""><span class="icon-recherche">Recherche</span></button>
            </fieldset>
        </div>
    </form>

<h3>liste des Stock</h3>
<div class="contenu">
    {% if stocks|length > 0 %}
        <table>
            <tbody>
            <tr>
                <td class="table_title">{{ knp_pagination_sortable(stocks, 'Nom', 'name') }}</td>
                <td class="table_title">Description</td>
                <td class="table_title">Action</td>
            </tr>
            {% for stock in stocks %}
                <tr>
                    <td class="interval">{{ stock.name }}</td>
                    <td class="interval">{{ stock.description }}</td>
                    <td class="interval"><a href="{{ path('stock_edit', { 'id': stock.id }) }}" class="icon-uni6C" title="Modification">MODIF</a>   <a href="" class="icon-uniE019" title="Suppression">SUPPR</a></td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        <div class="navigation">
            {{ knp_pagination_render(stocks) }}
        </div>
    {% else %}
        Pas de contenu disponible
    {% endif %}

</div>
</div>#}
