{{ dump (achat.createdAt|date('d/m/Y')) }}
<form action="{{ path("achat_edit", {'id': achat.id}) }}" method="post">
    <div class="login">
        <p>Modification d'un achat</p>
        {#<div>
            <label for="artclename">nom</label>
            <input type="text" id="artclename" name="_artclename" required="required" value="{{ article.name }}"/></td>
        </div>
        <div>
            <label for="articledescription">desccription</label>
            <textarea  id="articledescription" name="_articledescription">{{ article.description }}</textarea>
        </div>

        <div align="center">
            <input  class="btn-connex"  type="submit" id="_submit" name="_submit" value="Valider" />
        </div>#}

        <select name="_achatarticle">
            {% for article in articles %}
                <option value="{{ article.id }}" {% if (article.id == achat.article.id) %} selected {% endif %}>{{ article.name}}</option>
            {% endfor %}
        </select>
        Date Achat : <input type="text" name="_achatdate" class="datepicker" value="{{ achat.createdAt|date }}">
        Quantité : <input type="number" id="achatquantite" name="_achatquantite" required="required" value="{{ achat.quantite }}" /></td>
        Prix Unitaire : <input type="number" id="achatprixunitaire" name="_achatprixunitaire" required="required" value="{{ achat.prixUnitaire }}"/></td>

        <div align="center">
            <input  class="btn-connex"  type="submit" id="_submit" name="_submit" value="Valider" />
        </div>
    </div>
</form>
